@model List<ManagerDesk.ViewModels.DispatchViewModel>

<div class="row">
    @foreach (var dispatch in Model)
    {
        <div class="col-md-6 col-lg-3">
            @Html.Partial("DispatchCard", dispatch)
        </div>
    }
</div> 
