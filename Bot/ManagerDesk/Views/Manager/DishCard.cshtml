@model ManagerDesk.ViewModels.DishViewModel
<div class="col-md-6 col-lg-3">
    <div class="info-item">
        <h3 class="info-item-heading">@Model.Name</h3>
        <h4>@Model.Price р.</h4>
        <div>@Model.Description</div>
    </div>
</div>