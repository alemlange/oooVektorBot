@model ManagerDesk.ViewModels.TableCardViewModel
<div class="col-md-6 col-lg-3 flex">
    <div class="info-item">
        <h2>Стол №</h2>
        @foreach (var dish in Model.Orders)
        {
            <p>@dish.DishFromMenu.Name</p>
        }
    </div>
</div>