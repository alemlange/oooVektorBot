@model List<ManagerDesk.ViewModels.ModificatorViewModel>
<div class="row">
    @foreach (var mod in Model)
    {
        <div class="col-md-6 col-lg-3">
            @Html.Partial("ModificatorCard", mod)
        </div>
    }
</div>
